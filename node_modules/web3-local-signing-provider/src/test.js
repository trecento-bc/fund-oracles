// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');

function solution(n, a) {
  let max = 0
  let lastMax = 0
  let row = []
  for (let i = 0; i < n; i++) {
    row.push(0)
  }
  for (let i = 0; i < a.length; i++) {
    if (a[i] == n + 1) {
      lastMax = max
    } else {
      if(row[a[i] - 1] < lastMax) {
        row[a[i] - 1] = lastMax
      }

      max = Math.max(max, ++row[a[i] - 1])
    }
  }

  for(let i = 0; i < row.length; i++) {
    if(row[i] < lastMax) {
      row[i] = lastMax
    }
  }

  return row
}

// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');

function solution2(arr) {
  if(arr.length == 0) return 1

  let sol = []
  for(let i = 0; i < arr.length; i++) {
    sol[arr[i] - 1] = arr[i]
  }
  for(let i = 0; i < sol.length; i++) {
    if(!sol[i]) return i + 1
  }
  return sol.length + 1
}
/*

let arr = []
let n = 1e6
for (let k = 1; k <= n; k++) {
  if (k % 100 == 0) {
    arr.push(n + 1)
  } else {
    arr.push(Math.floor(Math.random() * n) + 1)
  }
}
*/

console.log(solution2([2, 3]))
console.log(solution2([2, 4, 3]))

